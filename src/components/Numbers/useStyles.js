import { makeStyles } from "@material-ui/core/styles";
const useStyles = makeStyles({
  question: {
    display: "flex",
    marginLeft: "5%",
    maxWidth: "200px",
  },
});
export default useStyles;
